# Profile: development (default). Safe for local work: no batch, dry-run on.
# Credentials still come from env or gitignored application-local.properties — not this file.

mail.batch-enabled=false
mail.dry-run=true
mail.test-send-enabled=false
logging.level.com.mailSender=INFO
logging.level.com.mailSender.smtp=INFO
