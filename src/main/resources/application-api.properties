# API / SaaS profile (M2-003). Enable with SPRING_PROFILES_ACTIVE=api
# (optionally comma-separated with development). Does not send mail on boot.
# Database and security are added in later Milestone 2 tasks; CLI remains the default.

mail.batch-enabled=false
mail.dry-run=true
mail.test-send-enabled=false
