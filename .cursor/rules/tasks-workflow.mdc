---
description: How to complete DEVELOPMENT_TASKS.md IDs (definition of done)
globs: "{DEVELOPMENT_TASKS.md,AGENT_WORKFLOW.md,AGENTS.md}"
alwaysApply: false
---

# DEVELOPMENT_TASKS.md completion

Follow `AGENT_WORKFLOW.md`. Per ID:

- Match the bullets under that ID in `DEVELOPMENT_TASKS.md`.
- Ship tests with the change when the behavior is testable (`mvn test`).
- Update README and `mail.*` config if you add keys or change defaults.
- Check off: `- [ ]` → `- [x]` (or `[!]` if blocked).

Stop and ask if the change would send mail, rewrite history, or go beyond the current ID.
